import React from 'react';
import Headers from '../components/Headers';

const MovieList = () => {
    return (
        <main>
            <Headers></Headers>
        </main>
    );
};

export default MovieList;